## Packages
framer-motion | Smooth animations for list entry and form interactions
zod | Schema validation (already in base but explicit mention for form)
react-hook-form | Efficient form state management
@hookform/resolvers | Zod integration for react-hook-form

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
